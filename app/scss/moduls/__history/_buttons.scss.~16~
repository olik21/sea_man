button{
	
	color: $white;
	font-family: $font-h;
	font-size: $font-h2-size;
	background: none;
	cursor: pointer;
	@include pad(10px);
	@include border-radius(3px);
	@include border(  $turquoise, $width: 3px);
	
	&:hover{
		@include opacity;
	}
	
}